# tinyua
A lightweight, from-scratch OPC UA client stack for .NET 8 — zero dependency on the OPC Foundation SDK. Implements the full binary protocol: encoding, 4 security policies, message chunking, secure channels, subscriptions, and a Fluent API for connecting to industrial OPC UA servers.
